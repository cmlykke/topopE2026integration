

-- 2026-08-28



<<<<<<<<<<<<<<<<<<<<<<<<< install the venv setup on another PC

----- check if venv is installed:

python -m venv --help

--- if not: install it

--- if it is:

go to cd D:\path\to\future\venvfolder

instyall uv:

powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

check the installation with: uv --version

if it doesnt exist, run:
$env:Path = "C:\Users\USER\.local\bin;$env:Path"

then place the two files ...setup-venv.ps1 and requirements.txt in the folder where
you want .venv to apear.

then run:

      Set-ExecutionPolicy -Scope Process Bypass
      .\20260828setup-venv.ps1









<<<<<<<<<<<<<<<<<<<< save venv installation for another PC

-- Copy the venv of the current machine, so i can recreate it:

'''
cd C:\Users\Bruger\IdeaProjects
.\.venv\Scripts\Activate.ps1
uv pip freeze | Out-File -Encoding utf8 C:\Users\Bruger\requirements.txt
'''

requirements.txt
Use the file produced by uv pip freeze above. That is the exact versions. If you have not frozen yet, this is the minimum set you installed.


setup-venv.ps1
create a file the above, and save it next to requirements.txt in the repo.


That script puts .venv at C:\Users\<you>\IdeaProjects\.venv, same layout as now, assuming the repo is C:\Users\<you>\IdeaProjects\topopE2026integration.
On the other machine

Install uv if needed.
Clone the repo into ...\IdeaProjects\topopE2026integration.
If scripts are blocked:

PowerShellSet-ExecutionPolicy -Scope CurrentUser RemoteSigned

Run:

PowerShellcd C:\Users\<you>\IdeaProjects\topopE2026integration
.\setup-venv.ps1

In PyCharm, set the interpreter to:

C:\Users\<you>\IdeaProjects\.venv\Scripts\python.exe

Add .venv/ and .jupyter-runtime/ to .gitignore if they are not there already. Commit requirements.txt and setup-venv.ps1 only.



